# Architecture

Discovery (>=5 candidates) → scoring → duplicate gate → research → source archive → rights gate → image enhancement → carousel/story rendering → QA → storage → official Meta container creation → publish → 24h/72h insights → weekly learning.

All live publishing is fail-closed and idempotent. GitHub is operational memory; external media storage holds public delivery assets.
